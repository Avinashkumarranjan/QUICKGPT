# Local dev (Windows)

- Backend: run `start-backend.cmd` (expects http://localhost:3000)
- Frontend: run `start-frontend.cmd` (opens http://localhost:5173)
- Both: run `start-all.cmd`

If you see "Backend not reachable" on the login page, the backend is not running on port `3000`.

